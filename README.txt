It is a repo to test how to correctly setup buildbot.

Add some text to trigger build.  And see what happens when have 2 build slaves
for the same builder.

Test latent build slave.  And test stopping the instance immediately after build.

Test GitHub notifications.
